import styled from "styled-components"

export const BottomTaskBarContainer = styled.div`
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    margin-left: var(--leftbar-width);
    margin-right: var(--rightbar-width);
    z-index: 5;
`
